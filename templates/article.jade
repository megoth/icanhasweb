extends layout

block append vars
  - bodyclass = 'article-detail'

block prepend title
  | #{ page.title + " - "}

block headScripts
  if !page.metadata.original && locals.url === "http://icanhasweb.net"
    script(type="text/javascript") !function(t,e,n){ function a(t){var  a=e.createElement("script");a.type="text/javascript" ,a.async=!0,a.src=("https:"===e.location.protocol?"https":"http")+":"+n,(t||e.body||e.head).appendChild(a)}function o(){var  t=e.getElementsByTagName("script"),n=t[t.length-1];return n.parentNode} var  p=o();t.spotId="sp_egjINSz4",t.parentElement=p,a(p)}(window.SPOTIM={},document,"//www.spot.im/launcher/bundle.js");

block header
  include author
  h1= page.title
  p.author
    | #{ "Written by " }
    mixin author(page.metadata.author)
    |, !{ moment.utc(page.date).format('DD. MMMM YYYY') } 
    if (page.metadata.original)
      | (
      a(href=page.metadata.original) Original blog
      |)

block content
  article.block-content
    section.content!= typogr(page.html).typogrify()
  nav
    ul
      li
        a(href=contents.index.url) « Full blog

block aside
  - var tags = env.helpers.getTagsFromArticle(page)
  include tagList
  if !page.metadata.original && locals.url === "http://icanhasweb.net"
    #spot-im-frame-inpage(data-post-id=page.__filename.replace('/index.md', '').split('/').pop())
